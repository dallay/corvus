{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "mem_get_observation",
  "description": "Fetch full observation content by memory ID.",
  "type": "object",
  "properties": {
    "input": {
      "type": "object",
      "properties": {
        "memory_id": {
          "type": "string",
          "minLength": 1,
          "description": "Memory ID to retrieve."
        },
        "include_deleted": {
          "type": "boolean",
          "description": "Whether to return the observation if it was deleted."
        }
      },
      "required": ["memory_id"],
      "additionalProperties": false
    },
    "output": {
      "type": "object",
      "properties": {
        "memory_id": { "type": "string" },
        "status": {
          "type": "string",
          "enum": ["active", "deleted"]
        },
        "observation": {
          "description": "The retrieved observation payload (as a raw JSON value)."
        },
        "metadata": {
          "type": "object",
          "additionalProperties": true
        }
      },
      "required": ["memory_id", "status"],
      "additionalProperties": false
    }
  },
  "required": ["input", "output"],
  "additionalProperties": false
}
