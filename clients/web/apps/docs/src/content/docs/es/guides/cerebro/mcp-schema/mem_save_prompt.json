{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "mem_save_prompt (draft/unimplemented)",
  "description": "Guardar un prompt en Cerebro (NotImplemented returning CerebroError::NotImplemented).",
  "type": "object",
  "properties": {
    "input": {
      "type": "object",
      "properties": {
        "prompt": {
          "type": "string",
          "description": "Texto del prompt a guardar."
        },
        "session_id": {
          "type": "string",
          "description": "Identificador de sesión opcional."
        },
        "tags": {
          "type": "array",
          "items": { "type": "string" }
        },
        "metadata": {
          "type": "object",
          "additionalProperties": true
        }
      },
      "required": ["prompt"],
      "additionalProperties": false
    },
    "output": {
      "type": "object",
      "properties": {
        "prompt_id": { "type": "string" },
        "status": { "type": "string", "enum": ["saved", "unimplemented"] }
      },
      "additionalProperties": false
    }
  },
  "required": ["input", "output"],
  "additionalProperties": false
}
