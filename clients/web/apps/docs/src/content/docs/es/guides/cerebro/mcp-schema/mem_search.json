{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "mem_search",
  "description": "Buscar memorias mediante consulta semántica o de texto.",
  "type": "object",
  "properties": {
    "input": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "minLength": 1,
          "description": "Consulta para búsqueda semántica/fuzzy (no vacía)."
        },
        "limit": {
          "type": "integer",
          "minimum": 1,
          "maximum": 100,
          "description": "Número máximo de resultados a devolver (por defecto 5)."
        },
        "scope": {
          "type": "string",
          "minLength": 1,
          "description": "Filtrar por ámbito (privado, compartido, etc.)."
        },
        "topic_key": {
          "type": "string",
          "minLength": 1,
          "description": "Filtrar por una clave de tema específica."
        },
        "include_deleted": {
          "type": "boolean",
          "default": false,
          "description": "Si se deben incluir registros marcados como eliminados."
        }
      },
      "required": ["query"],
      "additionalProperties": false
    },
    "output": {
      "type": "object",
      "properties": {
        "results": {
          "type": "array",
          "maxItems": 100,
          "items": {
            "type": "object",
            "properties": {
              "memory_id": { "type": "string", "minLength": 1 },
              "summary": { "type": "string", "minLength": 1 },
              "score": { "type": "number", "minimum": 0 },
              "topic_key": { "type": "string", "minLength": 1 },
              "scope": { "type": "string", "minLength": 1 },
              "timestamp": { "type": "string", "format": "date-time" }
            },
            "required": ["memory_id", "summary", "score", "topic_key", "scope", "timestamp"],
            "additionalProperties": false
          }
        },
        "truncated": {
          "type": "boolean"
        }
      },
      "required": ["results", "truncated"],
      "additionalProperties": false
    }
  },
  "required": ["input", "output"],
  "additionalProperties": false
}
