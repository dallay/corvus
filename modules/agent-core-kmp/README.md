# agent-core-kmp

Base Kotlin Multiplatform module for the future agent shared kernel.

Current bootstrap:

- `commonMain` + `commonTest`
- `jvm` target enabled
- `kotlin("test")` in `commonTest`

This module is intentionally minimal and ready for gradual expansion.
