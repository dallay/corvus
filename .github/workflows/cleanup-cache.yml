# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Cleanup caches

on:
  pull_request:
    types: [closed]

jobs:
  cleanup:
    permissions:
      actions: write
      contents: read
    uses: dallay/common-actions/.github/workflows/cleanup-cache.yml@74665f58fde40f4999fb56022bd05626f6d72076 # v2.1.0
    secrets: inherit
