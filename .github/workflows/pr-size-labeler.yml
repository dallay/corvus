# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: PR Size Labeler

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

jobs:
  label:
    permissions:
      contents: read
      issues: write
      pull-requests: write
    uses: dallay/common-actions/.github/workflows/pr-size-labeler.yml@87129475c06d9f3354d398426926ba4345a4644d # v2.2.3
