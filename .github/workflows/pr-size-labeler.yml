# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: PR Size Labeler

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

jobs:
  label:
    permissions:
      contents: read
      issues: write
      pull-requests: write
    uses: dallay/common-actions/.github/workflows/pr-size-labeler.yml@74665f58fde40f4999fb56022bd05626f6d72076 # v2.1.0
