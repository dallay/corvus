# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Greetings

on: [pull_request_target, issues]

jobs:
  greet:
    permissions:
      issues: write
      pull-requests: write
    uses: dallay/common-actions/.github/workflows/greetings.yml@87129475c06d9f3354d398426926ba4345a4644d # v2.2.3
    secrets: inherit
