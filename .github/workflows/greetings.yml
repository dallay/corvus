# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Greetings

on: [pull_request_target, issues]

jobs:
  greet:
    permissions:
      issues: write
      pull-requests: write
    uses: dallay/common-actions/.github/workflows/greetings.yml@6548f0dfaad9e40648cd56e85566e5bc2d707dfb # v2.2.2
    secrets: inherit
