# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Stale

on:
  schedule:
    - cron: "0 2 * * *"

jobs:
  stale:
    permissions:
      issues: write
      pull-requests: write
    uses: dallay/common-actions/.github/workflows/stale.yml@6548f0dfaad9e40648cd56e85566e5bc2d707dfb # v2.2.2
    secrets: inherit
