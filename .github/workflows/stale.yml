# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Stale

on:
  schedule:
    - cron: "0 2 * * *"

jobs:
  stale:
    permissions:
      issues: write
      pull-requests: write
    uses: dallay/common-actions/.github/workflows/stale.yml@74665f58fde40f4999fb56022bd05626f6d72076 # v2.1.0
    secrets: inherit
